# 测试环境配置
test:
  base_url: "http://localhost:8080"
  db:
    host: "localhost"
    port: 3306
    user: "root"
    password: "root"
    database: "blog_db"

# 生产环境配置
prod:
  base_url: "http://你的服务器IP"
  db:
    host: "你的服务器IP"
    port: 3306
    user: "root"
    password: "你的密码"
    database: "blog_db"
